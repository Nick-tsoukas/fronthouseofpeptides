{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build && if [ -d .output/public/_nuxt ]; then mkdir -p .output/server/chunks/public/_nuxt && cp -R .output/public/_nuxt/. .output/server/chunks/public/_nuxt/; fi"
  },
  "deploy": {
    "startCommand": "node .output/server/index.mjs",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
